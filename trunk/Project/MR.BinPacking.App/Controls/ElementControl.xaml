<UserControl x:Class="MR.BinPacking.App.Controls.ElementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             MinWidth="28"
             x:Name="elemControl">
    <Grid ToolTip="{Binding ElementName=elemControl, Path=Message}">
        <Border x:Name="Border" Background="{Binding ElementName=elemControl, Path=Color}" BorderBrush="Black" BorderThickness="0,1,0,0" />
        <Label x:Name="laSize" Content="{Binding ElementName=elemControl, Path=Message}" VerticalAlignment="Center" HorizontalAlignment="Center"
               FontWeight="Bold" Padding="0" Foreground="SteelBlue" Margin="4,0"/>
    </Grid>
</UserControl>
